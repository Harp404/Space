# Offline training only — none of this is needed to run the gateway or the demo.
numpy>=1.24
pandas>=2.0
scikit-learn>=1.3

# DINOv3 consequence classification (GPU). Weights are gated:
#   accept the licence at https://huggingface.co/facebook/dinov3-vitl16-pretrain-sat493m
#   then: huggingface-cli login
torch>=2.2
transformers>=4.44
pillow>=10.0

# Optional — dSGP4 validation (produces a benchmark number, ships nothing)
dsgp4>=1.2
